<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>산상수훈 암송 체크표</title><style>:root{font-family:system-ui,'Malgun Gothic',sans-serif;color:#24352d;background:#f6f8f3}*{box-sizing:border-box}body{max-width:1000px;margin:auto;padding:24px 14px 70px}header{background:#e3eee4;border-radius:22px;padding:24px;margin-bottom:18px}h1{margin:0 0 8px;font-size:1.9rem}p{line-height:1.6;margin:7px 0}.bar{display:flex;flex-wrap:wrap;gap:12px;align-items:center;margin:18px 0}input[type=date]{padding:10px;border:1px solid #a9bbaa;border-radius:9px;font:inherit}button{font:inherit;cursor:pointer;border:1px solid #88a290;background:#fff;border-radius:9px;padding:9px 12px}button:hover{background:#edf6ed}.meter{height:12px;background:#d7e1d7;border-radius:20px;overflow:hidden}.meter span{display:block;height:100%;background:#43835b;width:0}.week{background:white;margin:18px 0;padding:15px;border:1px solid #dce7dc;border-radius:17px}.week h2{margin:4px 2px 12px;font-size:1.2rem}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:9px}.day{display:flex;align-items:center;gap:12px;padding:14px;border:1px solid #e0e7df;border-radius:12px;cursor:pointer;min-height:76px}.day:hover{border-color:#6b9774}.day input{width:22px;height:22px;accent-color:#43835b;flex:none}.day strong{display:block}.day small{display:block;color:#587064;margin-top:3px}.done{background:#edf7ef}.review{background:#f7f3e7}.note{font-size:.89rem;color:#526a5b}@media print{body{padding:0;background:#fff}button{display:none}.week{break-inside:avoid}}</style></head><body><header><h1>📖 산상수훈 암송 체크표</h1><p>마태복음 5:1~7:29 · 월~토 하루 2절 · 일요일은 그 주 암송 범위 복습</p><div class="bar"><label>시작 월요일 <input id="start" type="date" value="2026-09-28"></label><button id="print">인쇄</button><button id="reset">체크 기록 초기화</button></div><p id="progress" aria-live="polite"></p><div class="meter" aria-hidden="true"><span id="fill"></span></div><p class="note">마지막 주 화요일은 1절입니다. 남은 날은 장별·전체 복습으로 구성했습니다. 체크 기록은 이 브라우저에만 저장됩니다.</p></header><main id="weeks"></main><script>
(()=>{'use strict';const KEY='sermon-memorization-v1',START='sermon-memorization-start-v1',lengths=[48,34,29],verses=[];lengths.forEach((n,i)=>{for(let v=1;v<=n;v++)verses.push({c:i+5,v})});const $=id=>document.getElementById(id),start=$('start');let checks={};try{checks=JSON.parse(localStorage.getItem(KEY)||'{}')||{};start.value=localStorage.getItem(START)||'2026-09-28'}catch(e){checks={}}function ref(a,b){return a.c===b.c?`마 ${a.c}:${a.v}~${b.v}`:`마 ${a.c}:${a.v}~${b.c}:${b.v}`}function fmt(date){return `${date.getMonth()+1}/${date.getDate()}`}function dateAt(offset){let [y,m,d]=start.value.split('-').map(Number);let x=new Date(y,m-1,d+offset);return fmt(x)}function update(){let boxes=[...document.querySelectorAll('.day input')],done=boxes.filter(x=>x.checked).length;$('progress').textContent=`완료 ${done} / ${boxes.length}일 · ${Math.round(done/boxes.length*100)}%`;$('fill').style.width=`${done/boxes.length*100}%`}function draw(){let main=$('weeks');main.replaceChildren();for(let w=0;w<10;w++){let sec=document.createElement('section');sec.className='week';let h=document.createElement('h2');h.textContent=`${w+1}주차 · ${dateAt(w*7)}~${dateAt(w*7+6)}`;sec.append(h);let grid=document.createElement('div');grid.className='grid';for(let d=0;d<7;d++){let index=w*6+d,review=d===6,range;if(w===9&&d>=2)range=['5장 전체 복습','6장 전체 복습','7장 전체 복습','5~7장 전체 연결 암송','산상수훈 전체 복습'][d-2];else if(review){let first=verses[w*12],last=verses[Math.min(w*12+11,110)];range=`${ref(first,last)} 복습`}else{let first=verses[index*2];let last=verses[Math.min(index*2+1,110)];range=ref(first,last)}let key=`${w}-${d}`,label=document.createElement('label');label.className='day'+(review?' review':'')+(checks[key]?' done':'');let input=document.createElement('input');input.type='checkbox';input.checked=!!checks[key];input.setAttribute('aria-label',`${w+1}주차 ${'월화수목금토일'[d]}요일 ${range} 완료`);input.addEventListener('change',()=>{if(input.checked)checks[key]=true;else delete checks[key];label.classList.toggle('done',input.checked);try{localStorage.setItem(KEY,JSON.stringify(checks))}catch(e){}update()});let content=document.createElement('span'),title=document.createElement('strong'),detail=document.createElement('small');title.textContent=`${'월화수목금토일'[d]}요일 · ${dateAt(w*7+d)}`;detail.textContent=range;content.append(title,detail);label.append(input,content);grid.append(label)}sec.append(grid);main.append(sec)}update()}start.addEventListener('change',()=>{if(!start.value)return;let [y,m,d]=start.value.split('-').map(Number),day=new Date(y,m-1,d);if(day.getDay()!==1){alert('월요일 날짜를 선택해 주세요.');start.value=localStorage.getItem(START)||'2026-09-28';return}try{localStorage.setItem(START,start.value)}catch(e){}draw()});$('reset').addEventListener('click',()=>{if(confirm('모든 체크 기록을 지울까요?')){checks={};try{localStorage.removeItem(KEY)}catch(e){}draw()}});$('print').addEventListener('click',()=>window.print());draw()})();
</script></body></html>
